import { ToolStrategy } from './model/strategy'
import { ToolType, Point, ToolSettingsMap, ToolCategory } from './model/types'

import { createTool } from './model/factory'

export type { Point, ToolStrategy, ToolType, ToolSettingsMap, ToolCategory }

export { createTool }
